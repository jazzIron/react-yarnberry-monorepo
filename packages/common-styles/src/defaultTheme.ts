import { ITheme } from './types';
import colors from './colors';
import fonts from './fonts';
import cssx from './style';

const theme: ITheme = {
  colors: colors,
  fonts: fonts,
  cssx: cssx,
};

export default theme;
